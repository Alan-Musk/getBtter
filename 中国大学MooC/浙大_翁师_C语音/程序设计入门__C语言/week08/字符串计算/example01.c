#include <stdio.h>
#include <string.h>

int main(int argc,char const *argv[]){
    // strlen函数输出长度不包含结尾的\0
    // char line[]="hello";
    // printf("strlen=%lu\n",strlen(line));
    // printf("sizeof=%lu\n",sizeof(line));
    
    // strcmp函数比较大小
    // char s1[]="abc";
    // char s2[]="adc";
    // printf("%d\n",strcmp(s1,s2));
    
    // strncpy函数,将s2复制到s1
    
    //strncat将s2拷贝到s1后面 返回s1 s1必须有足够的空间
    return 0;
}
